<nav class="navbar navbar-expand-lg">
    <a class="navbar-brand" href="/"><img src="/x/images/icon.png" class="align-top">TypicalBot</a>

    <div class="collapse navbar-collapse mr-auto">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="/">Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/documentation">Documentation</a>
            </li>
        </ul>
    </div>
    <div class="collapse navbar-collapse ml-auto">
        <ul class="navbar-nav">
            <li class="nav-item"><a class="nav-link" href="/invite">Add TypicalBot</a></li>
            <li class="nav-item"><a class="nav-link" href="/join-us">Join the Server</a></li>
            <li class="nav-item"><a class="nav-link" href="https://typicalbot.com/donate">Donate</a></li>
            <% if (auth) { %>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                        <% if (user.avatarURL) { %>
                            <img src="<%= user.avatarURL || `/x/images/missing.png` %>" class="align-top"><%= `${user.username}#${user.discriminator}` %>
                        <% } else { %>
                            <canvas class="user-avatar align-top" id="<%= user.id %>" style="margin-right: 5px;" data-name="<%= user.username %>" width="30" height="30"></canvas><%= `${user.username}#${user.discriminator}` %>
                        <% } %>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right">
                        <a class="dropdown-item" href="#"><i class="fa fa-user"></i> User Profile</a>
                        <a class="dropdown-item" href="#"><i class="fa fa-cogs"></i> Dashboard</a>
                        <!--<a class="dropdown-item" href="#"><i class="fa fa-flag"></i> Staff Panel</a>-->
                        <div class="dropdown-divider"></div>
                        <% if (user.lightTheme) { %>
                            <a class="dropdown-item" href="/user/theme/dark">Switch to Dark</a>
                        <% } else { %>
                            <a class="dropdown-item" href="/user/theme/light">Switch to Light</a>
                        <% } %>
                        <a class="dropdown-item" href="/auth/logout"><i class="fa fa-arrow-circle-left"></i> Log Out</a>
                    </div>
                </li>
            <% } else { %>
                <li class="nav-item">
                    <a class="nav-link" href="/auth/login">Log In</a>
                </li>
            <% } %>
        </ul>
    </div>
</nav>
